import { Shell } from '@/components/Shell';
import { characters, styles, references } from '@/lib/seed-data';
export default function Page(){
 const title = 'Characters';
 const data = title==='Characters'?characters:title==='Styles'?styles:references;
 return <Shell><div className="mb-6 flex items-center justify-between"><div><p className="text-sm font-bold text-amber-400">CRUD workspace</p><h2 className="text-3xl font-black">{title}</h2></div><button className="btn">New {title.slice(0,-1)}</button></div><div className="grid gap-4 xl:grid-cols-2">{data.map((item:any)=><article className="card" key={item.id}><h3 className="text-xl font-bold">{item.name}</h3><p className="mt-2 text-sm text-slate-300">{item.description || item.personality || item.type}</p><div className="mt-4 flex gap-2"><button className="btn-secondary">Edit</button><button className="btn-secondary">Duplicate</button><button className="btn-secondary">Delete</button></div></article>)}</div>{title==='Gallery' && <p className="card mt-6 text-slate-300">Generated panels will be grouped by chapter, scene, and panel number after image generation.</p>}{title==='Settings' && <div className="card"><h3 className="font-bold">Environment</h3><p className="mt-2 text-sm text-slate-300">Set OPENAI_API_KEY, NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY for production.</p></div>}</Shell>}
